# F01.9 — Run-time configuration

E01 · M2, M3, M5 · iOS, Android, Backend · [QACR-APP-SPEC-01](../../product/specs/QACR-APP-SPEC-01%20Rev1.2.md)

**Spec disposition:** as-is except D2

## Requirements owned

| Requirement | Milestone | Disposition | Evidence rows |
|---|---|---|---|
| FR-CFG-001 | M2 | as-is | 6 |
| FR-CFG-002 | M2 | as-is | 10 |
| FR-CFG-004 | M2 | as-is | 0 |
| FR-CFG-003 | M3 | departure D2 | 0 |
| FR-CFG-006 | M3 | as-is | 0 |
| FR-CFG-008 | M5 | as-is | 0 |
| FR-LCM-018 | M5 | as-is | 0 |

## What the vault records about the code

### FR-CFG-001

**qacr-android**

- **absent** — InitializationFlowState is declared as a nine-state enum with nothing driving it. — `AndroidQacr/app/src/main/java/io/healthy/qacr/model/State.kt:118-120`
- **absent** — Actions.UpdateInitConfiguration and the PreLogin action family are commented out. — `AndroidQacr/app/src/main/java/io/healthy/qacr/model/reducers/Actions.kt:53-59`
- **absent** — Actions.UpdateInitializationFlowState is declared but has no reducer case and no dispatch site. — `AndroidQacr/app/src/main/java/io/healthy/qacr/model/reducers/Actions.kt:85-87`

**qacr-ios**

- The server environment is hardcoded to .staging alongside the comment //TODO: Wait until Natrium integration. — `AppConfiguration+ServerEnvironment.swift:37-41`
- **scaffold** — APIKeys.Bootstrap.Response declares roughly 35 server-driven key names including privacyUrl, blockFlow, minValidFrames, sendPngImages, singleKit, supportNumber and resultsLobbyDuration, and no provider requests them. — `Quant/Infrastructure/Network/APIKeys.swift:97-136`
- PersistedSettings is a Codable struct with exactly three fields: sendPngImages, uploadFullFrame and isSoundMuted. — `urine.com.ios-qacr-app/Quant/AppLifecycle/AppConfiguration/PersistedSettings/PersistedSettings.swift:8-15`

### FR-CFG-002

**acr-android**

- **present** — FlowStartManager maps initializationFlowState == FAILED to SettingsFatalError and any value other than DONE to SettingsNoResponse. — `FlowStartManager.kt:87-92`

**acr-ios**

- The persisted settings struct declares build-time defaults such as lowDiskSpaceSize: Int64 = 80. — `AppConfiguration+PersistedSettings.swift:61`
- **absent** — The init response is parsed by hand-extracting 44 fields from a [String: Any] dictionary with per-field optional casts, with no top-level Decodable and no schema. — `AppSettingsNetworkService.swift:215-308`
- A customBlock without alert parameters produces a hard .internal failure. — `AppSettingsNetworkService.swift:234-240`
- An NSNull consultationConfig is explicitly guarded, with the reason given that serialising it would crash. — `AppSettingsNetworkService.swift:242-249`
- Four nested configs are decoded with JSONDecoder, and on decode failure the code logs, calls assertionFailure and returns nil. — `AppSettingsNetworkService.swift:318-364`
- AppConfiguration.updateInitValues(data:) is 44 assignments dominated by the pattern self.x = data.x ?? self.x. — `Dip/Features/Config/AppConfiguration.swift:93-135`
- **present** — canStartFlow() returns .failure(.networkIssueOrUnready) while ServerAppSettings.state is .loading or .unknown, and maps its .failure cases to blocking alerts. — `iosDip/Dip/Features/Lobby/Views/LobbyViewModel.swift:511-556`

**qacr-ios**

- The EnvironmentHealthCheck call in LobbyView is short-circuited by guard !appConfiguration.isProduction else { return }. — `Quant/Features/Lobby/LobbyView.swift:120-121`
- The environment health check's failure path only logs a warning. — `Quant/Features/Lobby/LobbyView.swift:126`

### FR-CFG-004

No rows recorded.

### FR-CFG-003

No rows recorded.

### FR-CFG-006

No rows recorded.

### FR-CFG-008

No rows recorded.

### FR-LCM-018

No rows recorded.

## Not covered by this spec

Nothing.

## Provenance

QACR-APP-SPEC-01 Rev 1.2 · FR-01 Rev 1.24 (brief cites 1.19) · EPIC-01 Rev 1.18 (brief cites 1.13)
